import React from 'react'
import AWSCloud from 'react-aws-icons/dist/aws/compute/AWSCloud';
import { Handle } from 'reactflow';

const AWSCloudIcon = () => <AWSCloud size={70} style={{ position: 'absolute' }} />


export default AWSCloudIcon 